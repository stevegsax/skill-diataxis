# Why the Basics Matter

A short explanation piece that exists only to give the upgrade script a file
in the explanation quadrant. No frontmatter by design.

The related equation $E = mc^2$ appears here so the math-delimiter rewrite
has something to chew on during the upgrade.
